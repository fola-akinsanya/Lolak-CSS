/* Add your JavaScript code here.

If you are using the jQuery library, then don't forget to wrap your code inside jQuery.ready() as follows:

jQuery(document).ready(function( $ ){
    // Your code in here
});

--

If you want to link a JavaScript file that resides on another server (similar to
<script src="https://example.com/your-js-file.js"></script>), then please use
the "Add HTML Code" page, as this is a HTML code that links a JavaScript file.

End of comment */ 

jQuery( ".dokan-save-attribute" ).html( "Save attributes" );

jQuery( ".dokan-attribute-variation-options h2" ).html( "Attributes" );


jQuery( ".dokan-attribute-variation-options .dokan-section-heading p" ).html( "Manage attributes for this  product." );
